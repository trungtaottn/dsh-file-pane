node_modules/
*.log
.tmp/
dist/
# local session handoff — never commit (see HANDOFF-next-session.md)
HANDOFF-next-session.md
# internal research/design reports — never commit (local-only)
plans/

# generated theme test bundle (rebuilt by pretest)
test/theme-dist/

# local-only agent context — never commit (real hostnames/ports/paths)
AGENTS.local.md
